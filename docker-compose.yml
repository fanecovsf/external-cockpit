services:
  frontend:
    build:
      context: .
      dockerfile: ./frontend/cockpit/Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    ports:
      - "1337:1337"
    restart: unless-stopped
